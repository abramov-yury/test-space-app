include favicon

doctype html

block variables

html(lang="ru")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    +favicon
    link(rel="manifest", href="manifest.json")
    title #{title}
  body
    block header
    block main
    block footer
