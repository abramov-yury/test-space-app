include ../feature/feature
include ../sign/sign

mixin intro

  section.intro
    h1.intro__title(lang="en") AI-Driven Analytics Platform
    .intro__features
      +feature
      ul.signs
        +sing("analysis")
        +sing("interpretation")
        +sing("visualization")
